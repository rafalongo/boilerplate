var gulp    = require('gulp');
var spriter = require('../util/spriter');

gulp.task('sprites', function() {
  
  /* 
   * para cada pasta que deseje converter em
   * sprite, basta duplicar a chamada abaixo.
   * 
   * certifique-se que a pasta exista
   * dentro de 'app/img'
   */

  // spriter('/img/ico/**/*.png');
  
});
